{
  "name": "better-lives-backend",
  "version": "1.0.0",
  "description": "better lives backend",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:pm2": "pm2 start app.js --watch && pm2 logs",
    "reload:pm2": "pm2 reload 0"
  },
  "author": "Bhoopendra Singh",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.1",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "mongoose": "^8.0.0",
    "nodemon": "^3.0.1"
  }
}
